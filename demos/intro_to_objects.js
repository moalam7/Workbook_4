"use strict";

//Properties are written as name and value pairs that are separated by a colon
let emp = {
  employeeId: "1",
  name: "Ezra",
  jobTitle: "Theater Teacher",
  payRate: 38.46,
};

//spacing, indentation, and line breaks are a matter of preference
let emp2 = { employeeId:"1", name:"Ezra", jobTitle:"Theater Teacher", payRate:38.46};

console.log(emp);
console.log(emp2);

console.log(employee.name,employee.jobTitle, employee.payRate);
